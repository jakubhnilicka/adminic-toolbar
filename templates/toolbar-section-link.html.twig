{% set attributes = create_attribute() %}
{% set classes = [
  active ? 'active',
] %}
<a {{ attributes.addClass(classes) }} href="{{ route }}">
  {{ title }}
  {% if badge %}<span class="badge">{{ badge }}</span>{% endif %}
</a>